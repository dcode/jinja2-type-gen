# Simple Example

This directory contains a standalone `uv` workspace project that demonstrates how to use the `jinja2-type-gen` library.

## Running the Example

Make sure you are in this directory, then run:

```bash
uv run example.py
```
